"""题目 py01:列方向 Softmax

框架会把数据准备成下面这样,你需要实现 forward:

    ctx.x        (rows, cols)  float32 CUDA 张量,**按行连续**(row-major)
    ctx.rows / ctx.cols        形状

要求:对**每一列**做 softmax,即沿 dim=0 归一化。

    out[r, c] = exp(x[r, c]) / sum_r' exp(x[r', c])

输出有两种写法,都合法:

    return tensor              # 惯用写法,推荐
    ctx.out.copy_(tensor)      # 框架预分配的缓冲;但这次拷贝本身要花一次全量访存

框架负责分配、搬运、计时、校验、越界检测。
注意:不要写 `ctx.out = ...` 重新赋值 —— 那样框架就看不到你的结果了。
"""

import torch


def forward(ctx):
    # ------------------------------------------------------------------
    # 先想清楚:这道题慢在哪?
    #
    # 基线是 torch.softmax(ctx.x, dim=0)。内核本身没问题 ——
    # 问题在于 x 按行连续:同一列的相邻元素在内存里相隔 cols 个 float,
    # 沿 dim=0 归约 = 每次取数都跨着一条 cache line。
    # 试着把"名义访存遍数"和"真实 cache line 数"分开算一算。
    #
    # 所以这道题不是"写个更快的 softmax",而是:
    #   **让归约发生在 PyTorch 能高效处理的维度上。**
    # ------------------------------------------------------------------

    # TODO: 实现它
    #
    # 几个值得自己想清楚的点:
    #
    #   1. x.t() 返回的是视图还是新张量?它花多少时间?
    #   2. 归约做完之后,结果要怎么变回 (rows, cols) 的形状?
    #   3. 数值稳定:softmax 要先减去最大值再取 exp。
    #      但**别**因此就自己手写 max/exp/sum/div 四步 —— 每一步都会物化
    #      一个完整的中间张量。先想想有没有现成的融合算子可以用。
    #   4. 输出别用 ctx.out.copy_(...):那次拷贝是一次实打实的全量访存,
    #      直接 return 更快。这道题会让你注意到多余拷贝的代价。
    #
    # 一个容易踩的坑:按列分块处理(x[:, j:j+blk])看起来"访存局部化",
    # 但那依然是不连续的视图,反而会慢很多。值得自己试一次看看数字。
    raise NotImplementedError("还没实现")
