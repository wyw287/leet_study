"""题目 py02:列方向标准化

框架会把数据准备成下面这样,你需要实现 forward:

    ctx.x      (rows, cols)  float32 CUDA 张量,**按行连续**(row-major)
    ctx.rows / ctx.cols        形状
    ctx.eps    float           数值稳定项(本题 1e-5)

要求:对**每一列**做标准化,即沿 dim=0 求均值与方差,再归一化:

    mu_c  = (1/rows) * Σ_r x[r, c]                   有偏均值
    var_c = (1/rows) * Σ_r (x[r, c] - mu_c)^2        有偏方差(除以 rows!)
    out[r, c] = (x[r, c] - mu_c) / sqrt(var_c + eps)

输出有两种写法,都合法:

    return tensor              # 惯用写法,推荐
    ctx.out.copy_(tensor)      # 框架预分配的缓冲;但这次拷贝本身要花一次全量访存

框架负责分配、搬运、计时、校验、越界检测。
注意:不要写 `ctx.out = ...` 重新赋值 —— 那样框架就看不到你的结果了。
"""

import torch


def forward(ctx):
    # ------------------------------------------------------------------
    # 先想清楚:这道题慢在哪?
    #
    # 基线是把公式拆成四个算子直接写出来:mean、var、减、除。
    # 每一步都正确,但 eager 模式下**每个算子都是一次独立的内核启动**,
    # 每次都要把这 268MB 从头读一遍、再把中间结果写回去。
    #
    # 所以这道题的第一个动作不是"优化",而是**算账**:
    # 拿笔数一遍,从 x 到 out 一共有几遍读、几遍写?
    # 哪些遍是真正必要的,哪些只是中间张量的搬运?
    #
    # 逐元素运算和归约的账要分开算 —— 它们的访存模式不一样。
    # ------------------------------------------------------------------

    # TODO: 实现它
    #
    # 几个值得自己想清楚的点:
    #
    #   1. 数一遍基线的访存遍数(单位取"读或写一遍 x")。再问:理论下限是几遍?
    #      中间那几遍分别来自哪个中间张量?
    #
    #   2. 归约那几遍能不能合并?均值与方差都是沿 dim=0 的归约,
    #      而 PyTorch 里有一类算子能**一趟同时**算出均值和方差 ——
    #      想想 torch.var_mean,或者去翻 torch 的归约类算子列表。
    #
    #   3. 逐元素那几遍呢?(x - mu) 再除以 sqrt(var+eps),
    #      能不能一趟做完"乘一个系数、再加一个偏置"?
    #      PyTorch 有一个三输入逐元素算子正好是这个形状。
    #
    #   4. 还有没有更狠的?如果某个**融合内核**一次的输入正好是
    #      「x + 沿某一维的均值 + 沿同一维的方差」,那它一遍就能出结果。
    #      想一想:哪个算子的语义和「标准化」几乎一样?
    #      它默认沿哪一维?我们要的又是哪一维?
    #      —— 提示到这里为止,自己去试。
    #
    # 注意 delta = eps 是加在**方差**上的,开方之后再除,别搞错顺序。
    # 有偏/无偏别选错(题目要求除以 rows)。
    #
    # 一个容易踩的坑:用 Python 循环逐列处理。每一列都会起一串内核 +
    # 一次同步,cols=8192 时慢到没有意义。PyTorch 的正确姿势永远是
    # 一次算完所有的列。
    raise NotImplementedError("还没实现")
